using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ship : MonoBehaviour {

    GameObject ship;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //Vector3 finalPosition = ship.transform.position;
        //finalPosition.x = Mathf.Clamp(finalPosition.x, -finalWidth, finalWidth);
        //finalPosition.y = Mathf.Clamp(finalPosition.y, -finalHeight, finalHeight);
        //ship.transform.position = finalPosition;
    }
}
